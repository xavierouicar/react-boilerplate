// import { fromJS } from 'immutable';
// import { makeSelectSuperheroesListDomain } from '../selectors';

// const selector = makeSelectSuperheroesListDomain();

describe('makeSelectSuperheroesListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
