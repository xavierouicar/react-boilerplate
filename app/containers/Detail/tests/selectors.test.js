// import { fromJS } from 'immutable';
// import { makeSelectDetailDomain } from '../selectors';

// const selector = makeSelectDetailDomain();

describe('makeSelectDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
